**🇷🇺 [Русский](README.md)** | **🇬🇧 [English](README_EN.md)**

---

# SubRP - RolePlay мод для BeamMP

> **⚠️ КРИТИЧЕСКОЕ ПРЕДУПРЕЖДЕНИЕ**: 
> 
> **СМОТРЕТЬ КОД ОЧЕНЬ ОПАСНО!** 
> 
> Весь код написан **топ мидл сеньером в Lua + API BeamMP**.  
> Это слив супер секретной разработки от **@Jastickon** для RP проекта в BeamMP.  
> 
> **Весь код создан с помощью DeepSeek и бесплатного ChatGPT.**
> 
> Плагин разработан для **BeamNG.drive версии 0.36**.
> 
> Все права **не защищены**. При попытке использования без согласия создателя **не будет** караться по закону!
> 
> **P.S.**: Качество кода говорит само за себя - видно, что писал "топ мидл сеньер", который даже не знает разницы между `==` и `=`, а комментарии в коде типа "работает, не трогай" - это пиздец!
> 
> **Избранные перлы кода:**
> - Защита от DDoS через счетчик IP - если больше 3 подключений, то это точно DDoS
> - Рефакторинг? А зачем, если можно просто добавить еще один `if`
> - Глобальные переменные везде, где только можно - зачем локальные, если можно глобальные?

Мод для BeamNG.drive с расширенной поддержкой ролевой игры (RolePlay) через протокол BeamMP. Полнофункциональная система для создания RP сервера с экономикой, бизнесами, работами, фракциями и достижениями.

## Архитектура

Проект организован по принципу **клиент-серверной архитектуры** с четким разделением ответственности:

### Структура уровней

```
SubRP/
│
├── subrp-client/                          # Клиентская часть мода
│   ├── lua/                                # Lua-скрипты мода
│   │   ├── ge/                             # Game Engine расширения
│   │   │   └── extensions/
│   │   │       ├── SubRPCore.lua           # Основной модуль клиента
│   │   │       ├── SubRPHud.lua            # Модуль HUD интерфейса
│   │   │       └── util/
│   │   │           └── richPresence.lua    # Discord/Steam Rich Presence
│   │   │
│   │   └── vehicle/                        # Vehicle расширения
│   │       └── extensions/
│   │           └── auto/
│   │               └── VSubRPCore.lua       # Синхронизация транспорта
│   │
│   ├── scripts/                            # Скрипты загрузки
│   │   ├── SubRPCore/
│   │   │   └── modScript.lua               # Загрузчик SubRPCore
│   │   └── SubRPHud/
│   │       └── modScript.lua               # Загрузчик SubRPHud
│   │
│   └── ui/                                  # Пользовательский интерфейс
│       └── modules/
│           ├── apps/
│           │   └── HudRP/                  # Основное HUD приложение
│           │       ├── app.html             # HTML интерфейс
│           │       ├── app.js               # JavaScript логика
│           │       ├── style.css            # Стили интерфейса
│           │       ├── images/              # Изображения (логотипы, иконки)
│           │       ├── sounds/              # Звуковые эффекты
│           │       └── fonts/               # Шрифты
│           └── loading/                     # Экран загрузки
│
└── subrp-server/                            # Серверная часть мода
    └── SubRPCore/                           # Основной модуль сервера
        ├── main.lua                         # Точка входа сервера
        ├── system/                          # Системные модули
        │   ├── DataManager.lua              # Управление данными (CRUD)
        │   ├── DataPlayer.lua               # Управление игроками
        │   ├── DataVehicle.lua              # Управление транспортом
        │   ├── DataWorking.lua              # Система работ
        │   ├── DataFraction.lua             # Управление фракциями
        │   ├── HudManager.lua               # Управление HUD данными
        │   ├── AchievementsManager.lua       # Система достижений
        │   ├── ApiEngine.lua                # API движок
        │   ├── CameraTrap.lua               # Камеры-ловушки (радары)
        │   ├── Commands.lua                 # Система команд
        │   ├── MapData.lua                  # Данные карты
        │   └── TriggerData.lua              # Данные триггеров
        │
        ├── anticheat/                       # Система античита
        │   ├── main.lua                     # Основной модуль античита
        │   ├── ac_core/
        │   │   ├── speedhack.lua            # Защита от читов скорости
        │   │   └── teleport.lua             # Защита от телепортов
        │   └── lib/
        │       └── logger.lua               # Логирование античита
        │
        └── libs/                             # Библиотеки
            ├── logger.lua                   # Система логирования
            ├── math.lua                     # Математические функции
            ├── start.lua                    # Инициализация
            ├── toml.lua                     # Парсер TOML
            └── ddos_protection.lua          # Защита от DDoS
```

### Принципы архитектуры

- **Разделение клиент-сервер**: вся бизнес-логика на сервере, клиент только отображает
- **Модульность**: компоненты организованы в независимые модули
- **Расширяемость**: поддержка опциональных компонентов и расширений
- **Безопасность**: встроенная система античита и защита от DDoS

### Поток данных

```
Клиент (BeamMP) → Сервер (SubRPCore) → DataManager → JSON файлы
                ↓
            HudManager → Клиент (UI обновление)
```

1. **Клиент**: отправляет события через `TriggerServerEvent`
2. **Сервер**: обрабатывает события в соответствующих модулях
3. **DataManager**: сохраняет/загружает данные в JSON файлы
4. **HudManager**: обновляет HUD данные клиента
5. **UI**: отображает обновленные данные игроку

## Основные модули

### Клиентские модули

#### `SubRPCore.lua` - Основной модуль клиента
**Назначение**: Обработка игровых событий на клиенте, синхронизация с сервером

**Основные функции**:
- `getPlayerVehicle()` - получение транспорта игрока
- `set_fuel(data_json)` - установка уровня топлива
- `add_fuel_station(data_json)` - заправка на АЗС
- `use_button()` - использование точек взаимодействия
- `VehSpawnPos(data)` - спавн транспорта
- `create_trigger_pos(data_json)` - создание триггеров
- `onSpeedTrapTriggered()` - обработка камер-ловушек
- `onRedLightCamTriggered()` - обработка камер на красный свет
- `load_points()` - отрисовка точек взаимодействия
- `RenderUsersNick()` - отрисовка ников игроков

**Особенности**:
- Управление топливом транспорта
- Система точек взаимодействия (АЗС, автосалоны, бизнесы)
- Визуализация точек на карте
- Синхронизация номерных знаков
- Отрисовка ников игроков с учетом расстояния

#### `SubRPHud.lua` - Модуль HUD интерфейса
**Назначение**: Управление пользовательским интерфейсом

**Основные функции**:
- `UpdateHudData(data_json)` - обновление данных HUD
- `UpdateMapLocation()` - обновление текущей локации
- `start_job_bus(route)` - начало работы водителем автобуса
- `stop_job()` - остановка работы
- `buy_car_diler(jbeam, color)` - покупка автомобиля
- `open_close_fast_use()` - быстрое взаимодействие с игроками
- `RunUINotify(data_json)` - отправка уведомлений
- `RunUISound(name, volume)` - воспроизведение звуков

**Особенности**:
- Автоматическое определение ближайшей локации
- Интеграция с Discord Rich Presence
- Система уведомлений
- Быстрое взаимодействие с игроками

#### `VSubRPCore.lua` - Vehicle расширение
**Назначение**: Синхронизация данных транспорта

**Основные функции**:
- `sendFuel()` - отправка данных о топливе
- `sendDamage()` - отправка данных о повреждениях

**Особенности**:
- Мониторинг уровня топлива
- Отслеживание повреждений транспорта

#### `richPresence.lua` - Rich Presence
**Назначение**: Интеграция с Discord/Steam для отображения статуса

**Особенности**:
- Отображение текущей локации
- Показ информации об игроке
- Кастомные изображения для карт

### Серверные модули

#### `DataManager.lua` - Управление данными
**Назначение**: CRUD операции с JSON файлами данных

**Основные функции**:
- `save_data(folder, filename, data)` - сохранение данных
- `load_data(folder, filename)` - загрузка данных
- `update_data(folder, filename, new_data)` - обновление данных
- `list_files(folder)` - список файлов в папке
- `info_count(folder)` - количество файлов

**Структура данных**:
- `players/` - данные игроков
- `vehicles/` - данные транспорта
- `buisnes/` - данные бизнесов
- `jobs/` - конфигурация работ
- `config/` - конфигурационные файлы
- `api/` - API данные
- `register/` - данные регистрации

#### `DataPlayer.lua` - Управление игроками
**Назначение**: Обработка подключения/отключения игроков, управление данными

**Основные функции**:
- `PlayerAuth()` - авторизация игрока
- `PlayerJoining()` - процесс подключения
- `PlayerJoin()` - успешное подключение
- `PlayerDisconnect()` - отключение игрока
- `PlayerSyncData()` - синхронизация данных игроков
- `SetConfigPerm()` - установка прав доступа

**Особенности**:
- Система регистрации через Discord
- Защита от множественных подключений с одного IP
- Автоматическое создание профиля при первой регистрации
- Система прав доступа (user, test, admin)
- Генерация уникального ID игрока
- Создание банковской карты

#### `DataVehicle.lua` - Управление транспортом
**Назначение**: Управление транспортом игроков

**Основные функции**:
- `buy_car_diler()` - покупка автомобиля в автосалоне
- `gen_place_number()` - генерация номерного знака
- `spawn_owned_vehicle()` - спавн транспорта игрока
- `FuelData()` - обработка данных о топливе
- `update_place_vehicle()` - обновление номерных знаков

**Особенности**:
- Генерация российских номерных знаков
- Система владения транспортом
- Интеграция с бизнесами (автосалоны)
- Отслеживание топлива

#### `DataWorking.lua` - Система работ
**Назначение**: Управление работой игроков

**Основные функции**:
- `start_job_bus()` - начало работы водителем автобуса
- `start_job_taxi()` - начало работы таксистом
- `stop_work()` - остановка работы
- `load_taxi()` - загрузка данных такси
- `getRandCheck()` - получение случайного чекпоинта

**Особенности**:
- Система маршрутов для автобусов
- Система уровней для таксистов
- Чекпоинты и награды
- Спавн рабочего транспорта

#### `HudManager.lua` - Управление HUD
**Назначение**: Управление данными HUD интерфейса

**Основные функции**:
- `send_data_hud(user_id)` - отправка данных HUD клиенту
- `load_user_data(user_id)` - загрузка данных игрока для HUD
- `update_hud_time()` - обновление времени и статистики
- `update_hud_online()` - обновление онлайн сервера
- `send_notify()` - отправка уведомлений
- `open_user_ui()` - открытие UI интерфейсов

**Управление бизнесами**:
- `buy_buisnes()` - покупка бизнеса
- `ChangeBuisnesAddBal()` - пополнение баланса бизнеса
- `ChangeBuisnesRemBal()` - снятие денег с бизнеса
- `ChangeBuisnesStatus()` - изменение статуса бизнеса
- `ChangeBuisnesRation()` - изменение коэффициента цен

**Финансовые операции**:
- `deposit_money()` - пополнение банковского счета
- `withdraw_money()` - снятие денег со счета
- `button_send_trans()` - перевод денег между игроками
- `button_pay_pdd()` - оплата штрафов ГИБДД
- `SubRPpayForFuel()` - оплата заправки

**Точки взаимодействия**:
- `SubRPPointUse()` - обработка использования точек
  - АЗС (fuelstations)
  - Автосалоны (cardealership)
  - Бизнесы (buisnes_buy)
  - Работы (start_work)

**Особенности**:
- Система уровней и опыта
- Отслеживание игрового времени
- Управление бизнесами
- Банковская система
- Система штрафов ГИБДД

#### `AchievementsManager.lua` - Система достижений
**Назначение**: Управление достижениями игроков

**Особенности**:
- Отслеживание прогресса достижений
- Награды за выполнение
- Интеграция с системой уровней

#### `ApiEngine.lua` - API движок
**Назначение**: Предоставление API для внешних систем

**Особенности**:
- API для получения списка игроков
- API статуса сервера
- Интеграция с Discord

#### `CameraTrap.lua` - Камеры-ловушки
**Назначение**: Система камер для контроля скорости и ПДД

**Особенности**:
- Камеры контроля скорости
- Камеры на красный свет
- Автоматическое наложение штрафов
- Отслеживание номерных знаков

#### `Commands.lua` - Система команд
**Назначение**: Административные команды сервера

**Особенности**:
- Команды для администраторов
- Управление игроками
- Управление сервером

#### `DataFraction.lua` - Управление фракциями
**Назначение**: Система фракций (полиция, медики и т.д.)

**Особенности**:
- Управление участниками фракций
- Система рангов
- Права доступа фракций

#### `MapData.lua` - Данные карты
**Назначение**: Управление данными карты

**Особенности**:
- Локации на карте
- Точки интереса
- Маркеры

#### `TriggerData.lua` - Данные триггеров
**Назначение**: Управление триггерами на карте

**Особенности**:
- Создание триггеров
- Обработка событий триггеров
- Синхронизация с клиентами

### Система античита

#### `anticheat/main.lua` - Основной модуль
**Назначение**: Координация работы античита

#### `anticheat/ac_core/speedhack.lua` - Защита от читов скорости
**Назначение**: Обнаружение и предотвращение читов скорости

#### `anticheat/ac_core/teleport.lua` - Защита от телепортов
**Назначение**: Обнаружение и предотвращение телепортов

### UI модули

#### `HudRP/app.html` - HTML интерфейс
**Назначение**: Структура HUD интерфейса

**Компоненты**:
- Информация об игроке (деньги, банк, уровень)
- Мини-карта с локациями
- Список игроков онлайн
- Уведомления
- Интерфейсы взаимодействия (АЗС, автосалон, бизнесы)

#### `HudRP/app.js` - JavaScript логика
**Назначение**: Логика работы UI интерфейса

**Особенности**:
- Обработка событий от сервера
- Обновление данных в реальном времени
- Звуковые эффекты
- Анимации

#### `HudRP/style.css` - Стили интерфейса
**Назначение**: Визуальное оформление интерфейса

## Зависимости

- **BeamNG.drive** версии **0.36** (плагин разработан для этой версии)
- **BeamMP** сервер и клиент
- **Lua** (встроенный в BeamNG.drive)
- **Node.js** (для серверной части, если требуется)

## Установка

### Установка клиентской части

1. Скопируйте папку `subrp-client` в директорию модов BeamNG.drive:
   ```
   <BeamNG.drive>/mods/subrp-client/
   ```

2. Убедитесь, что структура файлов сохранена

3. Запустите BeamNG.drive - мод загрузится автоматически

### Установка серверной части

1. Скопируйте папку `subrp-server` в директорию ресурсов BeamMP:
   ```
   <BeamMP Server>/Resources/subrp-server/
   ```

2. Добавьте ресурс в `ServerConfig.toml`:
   ```toml
   [Resources]
   SubRPCore = "subrp-server/SubRPCore"
   ```

3. Создайте необходимые папки для данных:
   ```
   SubRPDate/
   ├── players/
   ├── vehicles/
   ├── buisnes/
   ├── jobs/
   ├── config/
   ├── api/
   ├── register/
   └── fractions/
   ```

4. Настройте конфигурационные файлы в `SubRPDate/config/`

5. Перезапустите сервер BeamMP

## Использование

### Подключение к серверу

1. Запустите BeamNG.drive
2. Подключитесь к серверу BeamMP с установленным SubRP
3. Пройдите регистрацию (если требуется)
4. Начните игру в RP режиме

### Основные функции

#### Экономика
- **Деньги**: наличные и банковский счет
- **Банк**: пополнение, снятие, переводы
- **Бизнесы**: покупка, управление, получение прибыли
- **Работы**: заработок денег через работу

#### Транспорт
- **Покупка**: приобретение транспорта в автосалонах
- **Номера**: автоматическая генерация российских номеров
- **Топливо**: система заправки на АЗС
- **Повреждения**: отслеживание состояния транспорта

#### Работы
- **Автобус**: работа водителем автобуса с маршрутами
- **Такси**: работа таксистом с системой уровней
- **Грузоперевозки**: работа водителем грузовика

#### Бизнесы
- **Покупка**: приобретение бизнесов
- **Управление**: открытие/закрытие, изменение цен
- **Прибыль**: автоматический расчет прибыли
- **Материалы**: управление запасами

#### Фракции
- **Полиция**: патрулирование, штрафы
- **Медики**: лечение игроков
- **Другие**: расширяемая система фракций

#### Достижения
- **Прогресс**: отслеживание достижений
- **Награды**: получение наград за выполнение
- **Статистика**: отслеживание игрового времени, уровней

## Конфигурация

### Настройки сервера

Основные настройки находятся в файлах:
- `SubRPDate/config/posts.json` - точки взаимодействия
- `SubRPDate/config/level_config.json` - система уровней
- `SubRPDate/config/map_locations.json` - локации на карте
- `SubRPDate/config/sell_cars.json` - автомобили в продаже
- `SubRPDate/config/achievements_config.json` - достижения

### Настройки бизнесов

Файлы бизнесов находятся в `SubRPDate/buisnes/`:
- `station1.json`, `station2.json` - АЗС
- `cardealership1.json` - автосалоны

### Настройки работ

Файлы работ находятся в `SubRPDate/jobs/`:
- `bus.json` - маршруты автобусов
- `taxi.json` - конфигурация такси
- `cargo.json` - конфигурация грузоперевозок

### Настройки фракций

Файлы фракций находятся в `SubRPDate/fractions/`:
- `police.json` - конфигурация полиции

## Протокол коммуникации

Мод использует стандартный протокол BeamMP для коммуникации:

### События клиента → сервера

- `TriggerSubRPFuelData` - данные о топливе
- `TriggerSubRPPointUse` - использование точки взаимодействия
- `TriggerSubRPpayForFuel` - оплата заправки
- `TriggerSubRPChangeBuisnes*` - управление бизнесом
- `TriggerSubRPbuy_buisnes` - покупка бизнеса
- `Triggerbuy_car_diler` - покупка автомобиля
- `Triggerstart_job_bus` - начало работы автобусом
- `TriggerSubRPStartTaxi` - начало работы таксистом
- `Triggerstop_job` - остановка работы
- `Triggerbutton_send_trans` - перевод денег
- `Triggerbutton_pay_pdd` - оплата штрафа
- `Triggerfast_use_button` - быстрое взаимодействие
- `Triggerdeposit_money` - пополнение счета
- `Triggerwithdraw_money` - снятие денег

### События сервера → клиента

- `ClientPacketVehSpawnPos` - спавн транспорта
- `ClientPacketReloadSignals` - перезагрузка сигналов
- `ClientPacketReloadData` - перезагрузка данных
- `ClientEventUpdateHudData` - обновление HUD
- `ClientEventHudOpenUI` - открытие UI
- `ClientRunUINotify` - уведомление
- `ClientEventadd_fuel_station` - заправка
- `ClientEventset_fuel` - установка топлива
- `ClientEventSyncPlace` - синхронизация номеров
- `ClientEventcreate_trigger_pos` - создание триггера
- `ClientEventremove_trigger_pos` - удаление триггера
- `Clientcreate_object` - создание объекта
- `Clientremove_object` - удаление объекта

## Разработка

### Структура модулей

Каждый модуль следует паттерну:
```lua
local M = {}

-- Локальные функции
local function privateFunction()
    -- ...
end

-- Публичные функции
M.publicFunction = function()
    -- ...
end

-- Хуки
M.onUpdate = function(dt)
    -- ...
end

M.onExtensionLoaded = function() 
    -- ...
end

return M
```

### Добавление новых функций

1. **Клиентская часть**:
   - Добавьте функцию в соответствующий модуль (`SubRPCore.lua` или `SubRPHud.lua`)
   - Зарегистрируйте обработчик события через `AddEventHandler`
   - Обновите UI при необходимости

2. **Серверная часть**:
   - Зарегистрируйте событие через `MP.RegisterEvent`
   - Создайте функцию обработки
   - Используйте `DataManager` для работы с данными
   - Отправьте обновления клиенту через `MP.TriggerClientEvent`

### Добавление новых бизнесов

1. Создайте JSON файл в `SubRPDate/buisnes/`
2. Добавьте точку взаимодействия в `SubRPDate/config/posts.json`
3. Настройте логику в `HudManager.lua` → `SubRPPointUse()`

### Добавление новых работ

1. Создайте JSON файл в `SubRPDate/jobs/`
2. Добавьте точку взаимодействия в `SubRPDate/config/posts.json`
3. Реализуйте логику в `DataWorking.lua`
4. Добавьте UI интерфейс при необходимости

### Расширение UI

UI интерфейс реализован через HTML/CSS/JavaScript:
1. Измените `app.html` для структуры
2. Обновите `app.js` для логики
3. Настройте `style.css` для стилей
4. Добавьте новые изображения в `images/` при необходимости

## Отладка

### Логирование

Сервер использует систему логирования:
- `log_success(message)` - успешные операции
- `log_error(message)` - ошибки
- `log_warning(message)` - предупреждения
- `log_debug(message)` - отладочная информация

Клиент использует стандартную систему логирования BeamNG.drive:
- `log('I', 'tag', 'message')` - информационное сообщение
- `log('W', 'tag', 'message')` - предупреждение
- `log('E', 'tag', 'message')` - ошибка
- `log('D', 'tag', 'message')` - отладка

### Проверка данных

- Проверьте JSON файлы в `SubRPDate/`
- Используйте консоль разработчика в браузере для UI
- Проверьте логи сервера BeamMP
- Используйте `dump()` для вывода данных в лог

## Совместимость

- **BeamNG.drive**: **0.36** (плагин разработан для этой версии)
- **BeamMP**: последние версии
- **Моды**: совместим с большинством модов транспорта

## Безопасность

### Защита от читов

- Система античита (speedhack, teleport)
- Защита от DDoS
- Проверка данных на сервере
- Валидация всех событий

### Защита данных

- Все данные хранятся на сервере
- Клиент не имеет доступа к критическим данным
- Проверка прав доступа для всех операций

## Лицензия

**AGPL-3.0-or-later** (GNU Affero General Public License v3.0 or later)

Все права **не защищены**. При попытке использования без согласия создателя **@Jastickon** **не будет** караться по закону!

Эта лицензия разрешает:
- ✅ Использование кода в коммерческих и некоммерческих целях
- ✅ Модификацию кода
- ✅ Распространение кода
- ✅ Использование в частных проектах
- ✅ Все остальное, что только можно придумать